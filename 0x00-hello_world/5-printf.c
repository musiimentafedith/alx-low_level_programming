#include<stdio.h>
/**
 * main - a C program that uses printf to print a text to standard output.
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
