#include "main.h"

/**
 * _abs - computes the abosute value of a number
 * @n: is the number whose value to compute
 * Return: the abosule value of int
 */

int _abs(int n)
{
	if (n < 0)
		return (n);
	else
		return (-n);
}
